server:
  port: 7002

spring:
  application:
    name: livk-socket
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: livk.com:8848
        service: ${spring.application.name}

management:
  endpoints:
    web:
      exposure:
        include: '*'